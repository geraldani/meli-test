import React from 'react'

/**
 * Para que no quedara la barra de busqueda sola, agregue este pequeno mensaje para incitar a buscar algo =)
 */
const SearchMessage = () => (
  <div style={{marginTop: '24px', textAlign: 'center'}}>
    Para empezar, introduce algo en el buscador
  </div>
)

export default SearchMessage
